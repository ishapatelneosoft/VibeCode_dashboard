apiVersion: v1
kind: Service
metadata:
  name: auth-project-api
  labels:
    app: auth-project-api
spec:
  type: ClusterIP
  selector:
    app: auth-project-api
  ports:
    - name: http
      port: 80
      targetPort: http
